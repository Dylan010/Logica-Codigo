# Ejercicio Unique In Order

## Solución

La función recibe una secuencia (cadena o arreglo) y devuelve una lista de elementos sin ningún elemento con el mismo valor uno al lado del otro, preservando el orden original de los elementos. Itera sobre la entrada y agrega el elemento actual al resultado solo si es diferente al siguiente elemento.
