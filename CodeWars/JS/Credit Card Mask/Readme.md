# Ejercicio Credit Card Mask

## Solución

La función enmascara los caracteres de una cadena (como un número de tarjeta de crédito) reemplazándolos con el carácter "#", excepto los últimos 4 caracteres. Esto se logra iterando desde el inicio de la cadena hasta el índice `length - 4` y utilizando el método `.replace()`.
